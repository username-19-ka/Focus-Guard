# Hoist all packages — required for React Native / Metro to resolve modules correctly
node-linker=hoisted
# Allow pnpm to install even if lockfile version is incompatible with EAS's pnpm
frozen-lockfile=false

auto-install-peers=false
strict-peer-dependencies=false
# Allow pnpm to install even if the lockfile is out of date or version-incompatible.
# EAS build servers may run a different pnpm version; this prevents hard failures.
frozen-lockfile=false
